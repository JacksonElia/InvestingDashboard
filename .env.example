# Environment variables for development

# API URL for stock price fetching
# For local development with server.ts: http://localhost:3001
# For Vercel deployment: /api/stock-price (relative path, auto-detected)
# Can be explicitly set via vercel.json environment configuration
VITE_API_URL=/api/stock-price
